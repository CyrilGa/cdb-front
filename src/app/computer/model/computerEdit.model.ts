import {Company} from './company.model';

export class ComputerEdit {
  id: number;
  name: string;
  introducedDate: string;
  introducedHour: string;
  discontinuedDate: string;
  discontinuedHour: string;
  company: Company;
}
